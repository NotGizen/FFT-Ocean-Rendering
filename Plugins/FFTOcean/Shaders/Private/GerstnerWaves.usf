// VS main
void GerstnerVS( in float2 InPosition : ATTRIBUTE0, out float4 OutPosition : SV_POSITION)
{
    OutPosition = float4(InPosition, 0, 1);
}

// PS main
void GerstnerPS(in float4 InPosition : SV_POSITION, out float4 OutColor : SV_Target0)
{
    OutColor = float4(0,0,1,1);
}